{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build C RaspberryPi",
            "type": "shell",
            "command": "cmake --preset=raspberry_pi;cmake --build --preset=raspberry_pi"
        }
    ]
}